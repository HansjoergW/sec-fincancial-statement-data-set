"""
Just a dummy module to setup the build pipeline
"""

def addition(first_value: int, second_value: int) -> int:
    """
    Just a dummy function to setup the build pipeline
    :param a: first value
    :param b: second value
    :return: the sum of the two values
    """
    return first_value + second_value
